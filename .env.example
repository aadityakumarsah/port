# Bun dev server / Cloudflare Pages build — auto-loaded by Bun
PORT=3000

# Client-side Supabase — inlined into the browser bundle at build time
BUN_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
BUN_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
